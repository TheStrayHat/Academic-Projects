
  # Desktop app for data input

  This is a code bundle for Desktop app for data input. The original project is available at https://www.figma.com/design/QenRw4f60xwK5Am59OGS42/Desktop-app-for-data-input.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  